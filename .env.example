# Frontend (Vite) — only VITE_* vars are exposed to the browser.
# Base URL of the backend API. In dev the Vite proxy also forwards /api.
VITE_API_URL=http://localhost:5000/api
VITE_APP_NAME=EduMentor AI
# Google OAuth client ID (must match the backend GOOGLE_CLIENT_ID)
VITE_GOOGLE_CLIENT_ID=
